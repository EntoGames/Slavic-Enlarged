namespace = se_reform

# ═══════════════════════════════════════════════════════════════
# se_reform.0001 — Wielki Reformator
# Odpala się gdy reformator odłamu słowiańskiego kontrolował ≥9 z 15 miejsc świętych.
# Scope: character (reformator)
# ═══════════════════════════════════════════════════════════════
se_reform.0001 = {
	type = character_event
	title = se_reform.0001.t
	desc = se_reform.0001.desc
	theme = faith
	left_portrait = {
		character = root
		animation = personality_zealous
	}

	is_triggered_only = yes

	option = {
		name = se_reform.0001.a
		add_piety = 500
		add_prestige = 250
		add_character_modifier = {
			modifier = se_modifier_great_reformer
			years = 30
		}
	}
}

# ═══════════════════════════════════════════════════════════════
# se_reform.0003 — Stan Zerowy
# Odpala się gdy reformator kontrolował >= 6 i < 9 z 15 miejsc świętych.
# Scope: character (reformator)
# ═══════════════════════════════════════════════════════════════
se_reform.0003 = {
	type = character_event
	title = se_reform.0003.t
	desc = se_reform.0003.desc
	theme = faith
	left_portrait = {
		character = root
		animation = idle
	}

	is_triggered_only = yes

	option = {
		name = se_reform.0003.a
		# brak nagrody i kary — stan zerowy
	}
}

# ═══════════════════════════════════════════════════════════════
# se_reform.0002 — Pochopna Reformacja
# Odpala się gdy reformator kontrolował < 6 z 15 miejsc świętych.
# Scope: character (reformator)
# ═══════════════════════════════════════════════════════════════
se_reform.0002 = {
	type = character_event
	title = se_reform.0002.t
	desc = se_reform.0002.desc
	theme = faith
	left_portrait = {
		character = root
		animation = personality_cynical
	}

	is_triggered_only = yes

	option = {
		name = se_reform.0002.a
		add_piety = -300
		add_character_modifier = {
			modifier = se_modifier_hasty_reformer
			years = 15
		}
	}
}
